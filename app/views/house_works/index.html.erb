<div class='container'>
  <%= render "layouts/header" %>
  <div class='position-relative overflow-hidden p-3 p-md-5 text-center bg-light'>
    <h2>おてつだい登録</h2>
    <%= render "form", house_work: @house_work %>
    <div id="house_works">
      <% @house_works.each do |house_work| %>
        <%= render house_work %>
      <% end %>
    </div>
  </div>
</div>
